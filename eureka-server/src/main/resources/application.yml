server:
  application:
    name: eureka-server
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8070/eureka/ # Updated URL of Eureka Server 2
  server:
    wait-time-in-ms-when-sync-empty: 0


spring:
  application:
    name: eureka-server
